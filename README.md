# song-renamer

Running this program should be fairly straightforward. Make sure you have Python 3.14 and the `uv` package manager installed on your system. Navigate to the project root directory in your terminal of choice, and run 
```
uv run renamer.py [PATH TO FILE OR DIRECTORY]
```
To see the explanation of the program, just run.
```
uv run renamer.py help
```